apiVersion: apps/v1
kind: Deployment
metadata:
  name: altinn-assistant
  annotations:
    altinn.studio/image: altinn-studio-assistant:latest
    altinn.studio/image-tag: latest
  labels:
    release: altinn-assistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: altinn-assistant
  template:
    metadata:
      labels:
        app: altinn-assistant
    spec:
      serviceAccountName: altinn-assistant-sa
      containers:
        - name: altinn-assistant
          image: altinn-studio-assistant:latest
          ports:
            - containerPort: 8071
              name: http
              protocol: TCP
          resources:
            requests:
              cpu: 100m # Tested usage: 70m spike for 5 concurrent workflows
              memory: 250Mi # Tested usage: 220Mi
            limits:
              memory: 1Gi
          env:
            - name: AZURE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: altinn-assistant-eso-secret
                  key: AZURE_API_KEY
            - name: GITEA_BASE_URL
              value: "http://altinn-repositories-public"
            - name: LANGFUSE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: altinn-assistant-eso-secret
                  key: LANGFUSE_SECRET_KEY
            - name: LANGFUSE_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: altinn-assistant-eso-secret
                  key: LANGFUSE_PUBLIC_KEY
            - name: LANGFUSE_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: altinn-assistant-eso-secret
                  key: LANGFUSE_BASE_URL
            - name: LANGFUSE_ENABLED
              value: "true"
            - name: LANGFUSE_ENVIRONMENT
              valueFrom:
                secretKeyRef:
                  name: altinn-assistant-eso-secret
                  key: LANGFUSE_ENVIRONMENT
            - name: LANGFUSE_RELEASE
              value: "assistant-v1"
