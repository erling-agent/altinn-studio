apiVersion: v1
kind: Service
metadata:
  name: altinn-assistant
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8071
      protocol: TCP
      name: http
  selector:
    app: altinn-assistant
